<div class="books">
  {{#each books}}
    <div>
      <a href="http://localhost:3001/api/books/display/{{id}}">
        <img class="book-cover" src="{{image}}" alt="{{title}} book cover" />
                <span class="tooltip">{{title}}</span>
      </a>
    </div>
  {{/each}}
</div>

<div class="form-group" id="logout">
  <button class="logout">Log Out</button>
</div>
